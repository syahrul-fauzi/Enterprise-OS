# EIR Package Contract References

Contract YAML files referenced by this package.

- ELS Input schema contract: `contracts/transformations/t001-els-to-eir.contract.yaml` (root workspace contracts)
- EIR Output schema contract: defined in `src/interfaces.ts` EirRecord interface

This `contracts/` directory exists per frozen package structure.
