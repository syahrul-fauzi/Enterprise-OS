# Predicate Registry Contract References

Predicate contracts follow the frozen contract structure.

- All predicate declarations live in `src/index.ts` ALL_PREDICATES array
- Contract semantics enforced by transformation contracts at workspace root
